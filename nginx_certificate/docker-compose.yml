version: "2"
services:
  ngnix_1:
    build: .
    ports:
    - "443:443"
    - "80:80"
  backend:
    image: linoproject/web_apachephp_mysql
    ports:
      - "8080:80"
    volumes:
      - /<localpath>/test:/var/www/html/
